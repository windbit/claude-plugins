# Regression checklist (bug → guard)

Дисциплина: починил баг → строка сюда. Симптом / корень / фикс-коммит / guard (юнит-тест или
сценарий, который упадёт при возврате). Вернётся баг — падаем в `bun test` / на прогоне, не у юзера.

| Дата | Симптом | Корень | Фикс | Guard |
|---|---|---|---|---|
| 2026-07-18 | `/implement` иногда запускал чужой скилл (`oh-my-issues`) | `/`-автодополнение фаззи-матчит, Enter выбирает подсвеченный | `typeSlashCommand` (текст→Escape→Enter=литерал), `8f0d050` | ✅ e2e SK2 (`skills.md`): `/oh`→пейн «Unknown command: /oh» (литерал) |
| 2026-07-18 | несколько `/screen`/`/last` глючили | N циклов на пейн + коллизия temp-файла `screen-<pane>-<ms>` | one-view-per-pane + монотонное имя, `82fa8c5` | — (нужен: сценарий `live-views` [ЭКСКЛЮЗИВ] — два `/last`→первый удалён) |
| 2026-07-18 | пикер/пермишен/fallback терялись при рестарте хаба | стейт жил только в памяти | персист Stage 1–3 в `HubStateRepository`, `82fa8c5` и ранее | `tests/state-repo.test.ts` (round-trip + бэккомпат) ✅ |
| 2026-07-18 | разрешение всплывало дважды; канальный 🔐 в ЛС молча не доходил → висящий стейт | два независимых пути (канальный `doPermissionRequest` + picker) | убрал канальный путь целиком, picker — единственный UX, `0a9619c` | `tests/state-repo.test.ts` (нет бакета permissions) + e2e (только picker) |
| 2026-07-18 | удалённое поле стейта возвращалось на диск навсегда | конструктор `{...raw}` тащил старые ключи | pick известных ключей, `0a9619c` | `tests/state-repo.test.ts` «old file loads» ✅ |
| 2026-07-20 | после `/clear` (или TUI-`/resume`) биндинг хранил старый/пустой sessionId → рестарт возобновлял НЕ тот разговор (топик 2576 осиротел) | хаб узнавал id только при спавне и только для «свежей» сессии (`captureNewSessionId`); `/clear` спавна не делает | `syncSessionId` — хуки несут `session_id` каждый ход, хаб пишет его в биндинг при расхождении | e2e: `/clear` → лог `sessionId synced …316c1d00 → e9750137`, биндинг обновился |

| 2026-07-20 | сессия навсегда зависала на стартовом промпте («I am using this for local development»), чат мёртв | `ackStartupPrompts` жал Enter ОДИН раз на промпт и помечал его acked; если нажатие проглотилось (TUI ещё не принимал ввод, ~2с после запуска) — повтора не было никогда | перенёс ack в screen-loop: жмёт, пока промпт реально не исчез, и видит пейны БЕЗ подключённого stub (сессия на промпте ещё не подключена) | e2e: пейн застрял на промпте без stub → хаб залогировал `startup prompt auto-acked` и пейн прошёл дальше |

| 2026-07-21 | топик 447 зациклился: батч-ревью «Роль 2» прогонялось снова и снова (5 раз за 11 мин), между прогонами сессии не было | `learnCmdline` записывал argv ЛЮБОЙ подключившейся сессии; headless `claude -p '<промпт>'` с channel-флагами стал «командой запуска биндинга», а он одноразовый → оживление → прогон → выход → снова оживление | `isHeadlessArgv` — headless (`-p`/`--print`) argv не учится, `454baec` (PR #3); биндинг 447 вылечен вручную | ✅ юнит `isHeadlessArgv` в `core.test.ts` (петлевой кейс + интерактивные запуски, которые учить НУЖНО) |
| 2026-07-21 | на ПЕРВОМ запуске топика хаб писал «🚀 Запускаю заново», хотя ничего не перезапускалось | `mode==='new'` покрывает и `/new` поверх существующего разговора, и самый первый запуск — текст был один на оба | разделил по `binding.sessionId`: первый запуск → «🆕 Запускаю сессию» | — (текстовая строка, гоняется глазами на первом `/bind`) |

> Прочерк в Guard = долг: guard-тест ещё не написан, добить на проходе соответствующей фичи.
